name: cinema_booking_vendor
description: Vendor app for cinema booking demo
publish_to: "none"
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  image_picker: ^1.0.0
  shared_preferences: ^2.2.0

flutter:
  uses-material-design: true
